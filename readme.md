基本处理流程：
    步骤1：智能切片 (Chunking)
      └─ 按文档标题层级切分
      └─ 控制每块大小在1500 tokens左右
      └─ 保留完整的上下文信息

    步骤2：并发分析 (Concurrent Analysis)
      └─ 对每个块调用 LLM
      └─ 提取：摘要 + 表格 + 关键点
      └─ 多线程加速处理

    步骤3：结构化存储 (Structured Storage)
      └─ 统一 JSON 格式
      └─ 便于后续检索和分析
      └─ 支持可视化和报表生成